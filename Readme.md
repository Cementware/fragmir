# Fragmir.bz

Social interaction platform based on events and messaging

## Deployment

In order to deploy the development stack, run `docker compose up --build -d` on this folder, then head over to [the webui](http://localhost:5173)
