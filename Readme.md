# Fragmir.bz

Social interaction platform based on questions and events

## Deployment

In order to deploy the development stack, run `docker compose build --no-cache && docker compose up -d` on this folder, then head over to [the webui](http://localhost:5173). To destroy the docker environment run `docker compose down -v`
